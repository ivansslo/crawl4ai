services:
  - type: web
    name: crawl4ai-zapier-webhook
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 60
    envVars:
      - key: PORT
        value: "5000"
